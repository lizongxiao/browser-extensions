/**
 * 背景脚本通信模块
 * 
 * 该模块提供了与浏览器扩展背景脚本通信的功能。
 * 可用于发送消息、请求数据或触发背景脚本中的操作。
 */

/**
 * 发送消息给背景脚本
 * 
 * 该函数用于向扩展的背景脚本发送消息。
 * 目前处于注释状态，可根据需要打开。
 * 
 * @example
 * // 定期检查背景脚本是否活跃
 * sendBgMsg();
 */
export function sendBgMsg() {
  // 发送周期性消息检查背景脚本是否活跃
  // setInterval(() => {
  //   chrome.runtime.sendMessage({ action: "你在吗?背景脚本" });
  // }, 1000);
}
