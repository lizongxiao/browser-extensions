# 浏览器扩展集合使用指南

本项目是一个浏览器扩展集合，每个分支提供不同的功能。以下是不同功能分支的使用说明。

## 可用功能分支

- `dev-scroll-websize-top`: 网页置顶功能，提供滚动到页面顶部的按钮
- `dev-web-monitor`: 网页监控功能，监控网页资源加载和性能指标
- 更多功能分支正在开发中...

## 安装扩展

### 从 Chrome 应用商店安装（未发布前不可用）

1. 访问 Chrome 网上应用店
2. 搜索对应的扩展名称
3. 点击 "添加到 Chrome"

### 开发模式安装（本地调试）

1. 下载或克隆本仓库
2. 切换到需要的功能分支
   ```bash
   git checkout dev-scroll-websize-top  # 例如，切换到网页置顶功能分支
   git checkout dev-web-monitor  # 或者，切换到网页监控功能分支
   ```
3. 运行 `npm install` 安装依赖
4. 运行 `npm run build` 生成构建文件
5. 打开 Chrome 浏览器，访问 `chrome://extensions/`
6. 启用右上角的 "开发者模式"
7. 点击 "加载已解压的扩展程序"
8. 选择项目中的 `dist` 目录

## 各功能分支使用方法

### 网页置顶功能（dev-scroll-websize-top 分支）

1. 安装扩展后，浏览任何网页
2. 当页面滚动超过一定距离后，屏幕右下角会出现一个向上箭头按钮
3. 点击该按钮可以平滑地滚动回页面顶部
4. 点击浏览器工具栏中的扩展图标可以打开设置面板
5. 在设置面板中可以：
   - 启用/禁用滚动到顶部功能
   - 切换亮色/暗色主题

### 网页监控功能（dev-web-monitor 分支）

1. 安装扩展后，浏览任何网页
2. 扩展会自动开始监控页面资源加载和性能指标
3. 点击浏览器工具栏中的扩展图标可以打开监控面板
4. 在监控面板中可以：
   - 查看资源加载情况（JS、CSS、图片等）
   - 查看性能指标（加载时间、FPS等）
   - 导出监控数据
   - 配置监控参数

## 常见问题

**Q: 如何切换不同的功能扩展？**
A: 需要切换到对应的Git分支，重新构建并在浏览器中加载。不同分支提供不同的扩展功能。

**Q: 为什么我看不到滚动按钮？（网页置顶功能）**
A: 确保扩展已启用，并且页面已经向下滚动足够距离（超过 300 像素）。

**Q: 如何卸载扩展？**
A: 访问 `chrome://extensions/`，找到对应的扩展，点击 "删除" 按钮。

**Q: 滚动按钮会遮挡页面内容吗？（网页置顶功能）**
A: 滚动按钮设计为半透明且位于右下角，以尽量减少对页面内容的遮挡。如果仍有问题，可以通过扩展弹出窗口临时禁用该功能。

**Q: 监控功能会影响网页性能吗？（网页监控功能）**
A: 监控功能设计为轻量级，对网页性能的影响极小。如果发现明显的性能下降，可以通过扩展弹出窗口临时禁用部分监控功能。

**Q: 监控数据会被发送到远程服务器吗？（网页监控功能）**
A: 默认情况下，所有监控数据仅保存在浏览器本地。导出功能允许您将数据保存为本地文件，不会自动上传到任何服务器。

## 隐私说明

本扩展集合不收集任何个人数据。所有设置仅保存在您的本地浏览器中。 