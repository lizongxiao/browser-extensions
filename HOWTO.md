# ScrollWebSizeTop 使用指南

## 安装扩展

### 从 Chrome 应用商店安装（未发布前不可用）

1. 访问 Chrome 网上应用店
2. 搜索 "ScrollWebSizeTop"
3. 点击 "添加到 Chrome"

### 开发模式安装（本地调试）

1. 下载或克隆本仓库
2. 运行 `npm install` 安装依赖
3. 运行 `npm run build` 生成构建文件
4. 打开 Chrome 浏览器，访问 `chrome://extensions/`
5. 启用右上角的 "开发者模式"
6. 点击 "加载已解压的扩展程序"
7. 选择项目中的 `dist` 目录

## 使用方法

1. 安装扩展后，浏览任何网页
2. 当页面滚动超过一定距离后，屏幕右下角会出现一个向上箭头按钮
3. 点击该按钮可以平滑地滚动回页面顶部
4. 点击浏览器工具栏中的扩展图标可以打开设置面板
5. 在设置面板中可以：
   - 启用/禁用滚动到顶部功能
   - 切换亮色/暗色主题

## 常见问题

**Q: 为什么我看不到滚动按钮？**
A: 确保扩展已启用，并且页面已经向下滚动足够距离（超过 300 像素）。

**Q: 如何卸载扩展？**
A: 访问 `chrome://extensions/`，找到 ScrollWebSizeTop，点击 "删除" 按钮。

**Q: 滚动按钮会遮挡页面内容吗？**
A: 滚动按钮设计为半透明且位于右下角，以尽量减少对页面内容的遮挡。如果仍有问题，可以通过扩展弹出窗口临时禁用该功能。

## 隐私说明

本扩展不收集任何个人数据。所有设置仅保存在您的本地浏览器中。 